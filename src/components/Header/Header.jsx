import NavBar from './NavBar';

const Header = () => {
  return (
    <header className="header bg-gradient-to-r from-gray-200 to-gray-350 backdrop-blur-mds">
      <NavBar />
    </header>
  );
};

export default Header;
